apiVersion: k3d.io/v1alpha5
kind: Simple
metadata:
  name: capacity-cascade-platform
servers: 1
agents: 0
image: rancher/k3s:v1.35.8-k3s1
options:
  k3s:
    extraArgs:
      - arg: --disable=traefik
        nodeFilters:
          - server:*
